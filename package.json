{
  "name": "hello_cloud",
  "version": "0.1.0",
  "bin": {
    "hello_cloud": "bin/hello_cloud.js"
  },
  "scripts": {
    "build": "echo \"The build step is not required when using JavaScript!\" && exit 0",
    "cdk": "cdk",
    "test": "jest"
  },
  "devDependencies": {
    "aws-cdk": "2.42.1",
    "jest": "^27.5.1"
  },
  "dependencies": {
    "aws-cdk-lib": "2.42.1",
    "constructs": "^10.0.0"
  }
}
